import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("ucanduit")
                .font(.largeTitle)
            Text("Native Apple Edition")
                .foregroundStyle(.secondary)
        }
        .padding()
    }
}
